type DockerQuery {
  version: String @ifparent(field: "loaded")
  versions: DockerVersion @ifparent(field: "loaded")
  info: DockerInfo!
}

type DockerSubs {
  events: DockerEvent!
}

type Query {
  docker: DockerQuery!
}

type Subscription {
  docker: DockerSubs!
}